# Breast-Cancer-Prediction
 This project analyzes breast cancer data to build ML models for tumor classification (benign/malignant). It covers data cleaning, EDA, and training models like Random Forest and SVM, with performance optimization via GridSearchCV.
